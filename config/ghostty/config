# Config generated by Ghostty Config

# Configure clipboard behavior
clipboard-read = allow
clipboard-write = allow
copy-on-select = true

# Set window title font
window-title-font-family = SourceSans3VF

# Default font family
# NOTE: specific features are font-dependent
font-family = Monaspace Argon
font-feature = calt, ss01, ss02, ss03, ss09, liga

# Default font size
font-size = 14
font-synthetic-style =

# Disable bold fonts; explicitly set bold/bold-italic styles
font-style = Medium
font-style-bold = Medium
font-style-italic = Italic
font-style-bold-italic = Italic

# Set color theme
theme = nordfox

# Bind Shift+Enter for Claude Code
keybind = shift+enter=text:\x1b\r

# Enable cursor animation
custom-shader = shaders/cursor_warp.glsl
custom-shader = shaders/sonic_boom_cursor.glsl
